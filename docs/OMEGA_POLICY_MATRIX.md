# NeuralShell — OMEGA Policy Matrix (Level 5 Enforcement)

This document defines the definitive enforcement state of the OMEGA Intent Firewall and Kernel Brokers.

## 1. Intent Enforcement Matrix

| Intent ID | Role | Security Level | Requirement | Status |
| :--- | :--- | :--- | :--- | :--- |
| `llm:chat` | User | Level 1 (LOW) | Schema Validation | **ACTIVE** |
| `session:save` | User | Level 2 (MED) | AES-256 Encryption | **ACTIVE** |
| `kernel:net:fetch` | Agent | Level 4 (HIGH) | HTTPS + SPKI Pinning | **ACTIVE** |
| `kernel:agent:run` | Agent | Level 5 (CRITICAL) | TDD + Sandbox | **ACTIVE** |
| `kernel:proc:exec` | Kernel | Level 5 (CRITICAL) | SHA256 Hash Match | **ACTIVE** |

## 2. Kernel Capability Gating

The Microkernel (`@neural/omega-core`) enforces the following physical gates:

1.  **CAP_NET**:
    *   Zero-Network fallback for Renderer process.
    *   No raw `http://` allowed; all requests converted to `https://`.
    *   Global Response Ceiling: 5MB.
2.  **CAP_PROC**:
    *   Binary White-listing: Only binaries in `TASK_REGISTRY` can spawn.
    *   Zero-Env Inheritance: Child processes receive only `PATH` variable.
    *   Dynamic Sandbox: Agents restricted to `tmp/agent-scratchpad`.
3.  **CAP_KEYCHAIN**:
    *   Hardware-Bound Ed25519 node identity.
    *   Irrevocable Silicon Binding (CPU ID + Baseboard).

## 3. Governance Signals

*   **Policy Compliance**: 17/17 (`true`)
*   **Enforcement Mode**: OMEGA (Strict Fail-Closed)
*   **Sovereign Status**: **VERIFIED**

---
*Generated by NeuralShell OMEGA Core on ${new Date().toISOString()}*
