# NeuralShell Web Sandbox

This sandbox provides a 30-second in-browser proof run.

- Runtime target: WebContainer API when browser capabilities allow
- Fallback target: deterministic simulated proof logs
- Demo source snapshot: `web-sandbox/sandbox/demo-src`

The GitHub Pages runtime entrypoint is published at `docs/web-sandbox/index.html`.
