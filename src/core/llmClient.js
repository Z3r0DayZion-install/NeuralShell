/**
 * Deprecated LLM client. This file remains for backwards compatibility,
 * but its functionality has been replaced by the LLMSupport
 * implemented in `llmService.js`. It exports an empty object so that
 * requires do not fail if still referenced in legacy code paths.
 */
module.exports = {};
